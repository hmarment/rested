[configuration]

name = "amp"
base-url = "https://amp.adsquare.com/api/v1"

    [configuration.auth]
    
    type = "JWT"
    login-resource = 'auth'
    login-method = 'login'

[resources]

    [resources.auth.login]

    endpoint = "/v1/auth/login"
    method = "POST"
    arguments = ["username", "password", "rememberMe"]
    authenticate = false

    [resources.audienceProximity]

    [resources.audiences]